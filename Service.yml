apiVersion: v1
kind: Service

metadata:
  name: my-frontend-service

spec:
  type: NodePort

  selector:
    app: my-frontend

  ports:
    - protocol: TCP
      port: 8082
      targetPort: 80
      nodePort: 31200